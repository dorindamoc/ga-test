import { colors } from '@assets/styleConstants'
import styled from 'styled-components'

const TVText = styled.div`
    color: ${colors.captureBlue};
`

export const TVProductPage = () => <TVText>This is the TV page</TVText>
