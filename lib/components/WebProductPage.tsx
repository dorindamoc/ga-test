import { colors } from '@assets/styleConstants'
import styled from 'styled-components'

const WebText = styled.div`
    color: ${colors.captureBlue};
`

export const WebProductPage = () => <WebText>This is the web page</WebText>
